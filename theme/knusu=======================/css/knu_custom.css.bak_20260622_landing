:root {
    --knu-deep-green: #0f3b32;
    --knu-charcoal: #1f242b;
    --knu-gold: #d7b372;
    --knu-gold-strong: #c89a47;
    --knu-bg: #f3f4ef;
    --knu-text: #1f2630;
    --knu-muted: #5f6977;
    --knu-border: #e7ebf0;
    --knu-shadow: 0 16px 36px rgba(18, 32, 44, 0.12);
    --knu-radius: 18px;
}

/* Common Layout */
.knu-main {
    background:
        radial-gradient(circle at 85% -10%, rgba(215, 179, 114, 0.22), transparent 42%),
        radial-gradient(circle at -10% 35%, rgba(15, 59, 50, 0.08), transparent 32%),
        var(--knu-bg);
    color: var(--knu-text);
}

.knu-container {
    width: min(1240px, calc(100% - 40px));
    margin: 0 auto;
}

.knu-section {
    padding: 108px 0;
}

.bg-light { background: #fff; }
.bg-alt { background: #f8fafb; }
.bg-dark {
    background:
        linear-gradient(130deg, rgba(255,255,255,0.05), transparent 50%),
        linear-gradient(135deg, #0f3b32 0%, #1f242b 100%);
    color: #fff;
}

.knu-sec-head {
    text-align: center;
    margin-bottom: 52px;
}

.knu-sec-head h3 {
    margin: 0 0 14px;
    font-size: clamp(30px, 4.1vw, 50px);
    line-height: 1.2;
    letter-spacing: -0.03em;
    font-weight: 900;
    color: var(--knu-charcoal);
}

.knu-sec-head p {
    margin: 0;
    font-size: clamp(16px, 1.8vw, 20px);
    line-height: 1.75;
    color: var(--knu-muted);
}

.bg-dark .knu-sec-head h3,
.bg-dark .knu-sec-head p {
    color: #fff;
}

.bg-dark .knu-sec-head p {
    opacity: 0.85;
}

/* Common Button */
.knu-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    min-height: 56px;
    padding: 0 30px;
    border-radius: 12px;
    border: 0;
    font-size: 17px;
    font-weight: 800;
    text-decoration: none;
    transition: transform .24s ease, box-shadow .24s ease, filter .24s ease, background .24s ease, color .24s ease;
}

.knu-btn:hover {
    transform: translateY(-3px);
}

.knu-btn-gold {
    color: #181818;
    background: linear-gradient(135deg, #f0d39a, var(--knu-gold-strong));
    box-shadow: 0 12px 26px rgba(200, 154, 71, 0.36);
}

.knu-btn-glass {
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.62);
    background: rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(5px);
}

.knu-btn-dark {
    color: #fff;
    background: linear-gradient(135deg, #202b35, #131920);
    box-shadow: 0 10px 24px rgba(12, 20, 30, 0.3);
}

.knu-btn-sm {
    min-height: 42px;
    padding: 0 16px;
    font-size: 14px;
    background: #f0f3f6;
    color: var(--knu-charcoal);
}

.knu-btn-sm:hover {
    background: #e1e8ef;
}

.knu-hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
}

/* Common Motion */
[data-knu-fade] {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity .72s cubic-bezier(0.2, 0.8, 0.2, 1), transform .72s cubic-bezier(0.2, 0.8, 0.2, 1);
}

[data-knu-fade].is-show {
    opacity: 1;
    transform: translateY(0);
}

@media (max-width: 768px) {
    .knu-section {
        padding: 76px 0;
    }

    .knu-container {
        width: min(1240px, calc(100% - 26px));
    }
}
